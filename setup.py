
from setuptools import setup

setup(name='Orca_scripts',
        version='0.1',
        description='Contains routines for pasing data from a ORCA output files',
        packages=['Orca_parse'],
        zip_safe=False
        )
